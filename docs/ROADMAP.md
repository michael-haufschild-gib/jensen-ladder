# Roadmap

Gates before Lean work starts:
- G1: statement + file-ownership division agreed between both agents
  (mailbox F238/F241).
- G2: numerical pre-certification freeze — certificate-grade interval
  enclosures of the moments (target relative width ≤ 1e-12 for k ≤ D₀)
  and the collision-barrier ladder harvest fixing D₀ (64, stretch 96).

Layers and provisional ownership (FILE-CLAIM to be confirmed by mailbox):
- Moments.lean       — Fable
- Sections.lean      — Fable
- Sturm.lean         — GPT
- Main.lean          — joint
- scripts/verify.py  — numerical re-derivation suite (both)

Paper: certified theorem + clearly-labeled numerical phenomenology
(edge localization of the collision obstruction) + the (T1)+(T2)
asymptotic roadmap. Repository goes public only on Michael's explicit go.
