import JensenLadder.Basic
import JensenLadder.XiJensen
import JensenLadder.RHReduction
import JensenLadder.ModelToXiTransfer
import JensenLadder.PolyaSchurTransfer
import JensenLadder.MagicInterpolationBridge
import JensenLadder.ResolutionWall
import JensenLadder.CCMRankOne
import JensenLadder.CCMQuotient
import JensenLadder.CCMFiniteWeil
import JensenLadder.CCMPerturbationBound
import JensenLadder.CCMFrobeniusBound
import JensenLadder.CCMFiniteKatoBridge
import JensenLadder.PrimeLocalNoGo
import JensenLadder.PrimeDominationBabyCarrier
import JensenLadder.HodgeSignatureBabyCarrier
import JensenLadder.GraphLaplacianBabyCarrier
import JensenLadder.GlobalDominationReduction
import JensenLadder.DiagonalBudgetSharpness
import JensenLadder.RowDiagonalDominationCarrier
import JensenLadder.SchurStarDominationCarrier
import JensenLadder.SchurStarSharpness
import JensenLadder.SchurStarAssemblyCarrier
import JensenLadder.ResidualSchurSelector
import JensenLadder.AbsoluteBudgetFakeFamilyBlindness
import JensenLadder.AdversarialSignTwistNoGo
import JensenLadder.SpectralRealization
import JensenLadder.SpectralFaithfulnessGap
import JensenLadder.MomentProblemCarrier
import JensenLadder.DeningerCarrier
import JensenLadder.ComplexSpectralDeterminantCarrier
import JensenLadder.DeningerFlowUnitary
import JensenLadder.FriedDeningerTorsionCarrier
import JensenLadder.ArithmeticSiteCarrier
import JensenLadder.HodgeIndexCarrier
import JensenLadder.HodgeIndexLorentzian
import JensenLadder.PrimeResonanceFreeness
import JensenLadder.T3T5Stage0Package
import JensenLadder.T3T5Stage3Referee
import JensenLadder.T3T5Stage4ModularJ
import JensenLadder.T3T5SumRule
import JensenLadder.WeilFluctuationLoewner
import JensenLadder.T3T5Capstone
import JensenLadder.HodgeWeilBridge
import JensenLadder.PrimitiveHodgeWeilEngine
import JensenLadder.PrimitiveHodgeWeilCalibration
import JensenLadder.PrimeLocalHodgeNoGo
import JensenLadder.GeometricSquareRootCarrier
import JensenLadder.FiniteCarrierNoGo
import JensenLadder.TruncationLimitCarrier
import JensenLadder.UnitaryCokernelCarrier
import JensenLadder.SpectralReflection
import JensenLadder.MorseCriterion
import JensenLadder.BochnerTopRoute
import JensenLadder.MorseDeningerBridge
import JensenLadder.CVSSpectralRoute
import JensenLadder.DerivativeBasepointConvergence
import JensenLadder.DeterminantHurwitzRoute
import JensenLadder.SquaredDeterminantFinite
import JensenLadder.SquaredDeterminantGauge
import JensenLadder.SquaredVariablePullback
import JensenLadder.FredholmSquaredCarrier
import JensenLadder.BurnolCriticalSystems
import JensenLadder.MeyerForkASquaredDeterminant
import JensenLadder.DiagonalFredholmProduct
import JensenLadder.SquaredDeterminantApproximation
import JensenLadder.SquaredDeterminantZeroTransfer
import JensenLadder.SquaredDeterminantSpectralProduct
import JensenLadder.ChiralMomentPSD
import JensenLadder.ChiralStieltjesTrace
import JensenLadder.ChiralMomentReconstruction
import JensenLadder.ChiralSourceMomentGate
import JensenLadder.ChiralSourceTraceReconstruction
import JensenLadder.PrimeCepstrumHankelCarrier
import JensenLadder.ChiralExplicitFormulaSource
import JensenLadder.EulerLoadedPrimitiveProjection
import JensenLadder.SquaredDeterminantPairedSpectralProduct
import JensenLadder.SquaredDeterminantHurwitzBridge
import JensenLadder.HurwitzRealRootedLimit
import JensenLadder.CanonicalProductGenusOne
import JensenLadder.CanonicalProductFactorZeros
import JensenLadder.GenusOneDeterminantControl
import JensenLadder.ScatteringParityNoGo
import JensenLadder.CCMGroundStateRoute
import JensenLadder.CCMGroundStateError
import JensenLadder.CVSGapPreservation
import JensenLadder.CVSKatoWeyl
import JensenLadder.CVSFiniteGapScreen
import JensenLadder.CVSKatoSmallNoGo
import JensenLadder.CVSMarginalLimit
import JensenLadder.MonotoneNoMargin
import JensenLadder.SpectralPollutionFreeLimit
import JensenLadder.OddMorseCertificate
import JensenLadder.StructureTheorem
import JensenLadder.T1Phase
import JensenLadder.T2Edge
import JensenLadder.BAH1Quadratic
import JensenLadder.BAH1PrimeRow
import JensenLadder.DHMultiplicityFakeGate
import JensenLadder.FiniteMultiplicityVisibilityNoGo
import JensenLadder.FiniteEulerProductMixedLogGate
import JensenLadder.VonMangoldtPrimePowerSeparator
import JensenLadder.FiniteMixedLogVisibilityNoGo
import JensenLadder.E1K
import JensenLadder.LogTransfer
import JensenLadder.CauchyTransfer
import JensenLadder.MomentLogError
import JensenLadder.SDCMargin
import JensenLadder.ContourLegality
import JensenLadder.AffineContour
import JensenLadder.IntegrandHolomorphy
import JensenLadder.CorridorGeometry
import JensenLadder.SDCertificate
import JensenLadder.PacketForce
import JensenLadder.MomentTraceBridge
import JensenLadder.C2PComparison
import JensenLadder.C2PLowerRow
import JensenLadder.C2PResponseNorm
import JensenLadder.C2PSuzukiMass
import JensenLadder.SuzukiPhase
import JensenLadder.C2PFullCertificate
import JensenLadder.Sturm

-- Analytic-brick / standalone modules (previously outside the import closure);
-- imported here so a clean `lake build` verifies every source module.
import JensenLadder.RvMXiEntire
import JensenLadder.RvMXiLink
import JensenLadder.ZeroCountingN
import JensenLadder.XiOrderBound
import JensenLadder.XiZeroCounting
import JensenLadder.W1DensityNevanlinna
import JensenLadder.CompletedZetaStripBound
import JensenLadder.XiEntireEven
import JensenLadder.CarrierCanonicalProduct
import JensenLadder.ChiralDiracSquare
import JensenLadder.DeterminantNormalizationNoGo
import JensenLadder.LiCriterionDisk
import JensenLadder.ModelTuranRatio
import JensenLadder.SymmetryProtection
import JensenLadder.W3InnerFactor

-- Operator-algebraic / universality no-go family (amenability, modular quotient,
-- prime-quarantine trace): newer obstructions propagated from the workspace ledger.
import JensenLadder.AmenabilityNoGo
import JensenLadder.ModularQuotientNoGo
import JensenLadder.PrimeQuarantineTraceNoGo

-- Secular (Cauchy/Aronszajn) structure of the CCM-route reconstruction:
-- det(M - z•1) factorization; eigenvalue ⟺ secular equation (worklog C-D8).
import JensenLadder.SecularReconstruction

-- Secular ground state: S(z)=∑aₙ/(dₙ−z) strictly increasing + injective below the poles
-- (positive weights) ⟹ the bottom/ground-state root is unique. RH-free ordering structure of the
-- Cauchy/Aronszajn kernel underlying the CCM ζ-bottom certification (Stage 1, T3/T5 sub-program).
import JensenLadder.SecularGroundState

-- Hodge-index inequality (Lorentzian / "one positive direction"): the RH-free finite core of
-- Stage 0/2 of the T3/T5 carrier-construction sub-program.
import JensenLadder.HodgeIndexInequality

-- Positive secular weights ⟹ real secular spectrum (finite Lee–Yang core): the RH-free backbone
-- of the Herglotz reduction; ferromagnetic positive residues localize roots.
import JensenLadder.SecularRealRoots
import JensenLadder.SecularSignedRealRoots
-- Secular m-function Herglotz dichotomy: positive residues map the upper half-plane to itself, while a
-- positive off-line atom breaks Herglotz. De Branges/canonical-system continuation structure, margin-free.
import JensenLadder.SecularHerglotz
-- Self-adjoint resolvent m-function is Herglotz (Im⟨v,(A−z)⁻¹v⟩ = Im z·‖·‖² > 0): the fundamental de
-- Branges object for the actual real-symmetric Q_W (not the positive-residue model). RH-free.
import JensenLadder.ResolventHerglotz

-- ±-quadruple structural fact: a matrix similar to its negative has spectrum closed under negation
-- (the F D′F = -D′ even-symmetry skeleton of the secular reconstruction's ½±iγ Klein-4 structure).
import JensenLadder.SpectrumNegSymm

-- Concavity of the Weil-bottom in the prime-weight scaling: λ_min(A−tB) is concave in t (inf of
-- affine Rayleigh functionals) ⟹ unique max; the variational backbone (prime-weight variational doc).
import JensenLadder.WeilBottomConcave

-- T5 first stone: the two Frobenius ruling axes of the self-product span a hyperbolic (1,1) plane
-- (isotropic f₁,f₂ ⟹ Q(af₁+bf₂)=ab·polar; ℓ=f₁+f₂ ample) — the surface structure of Spec ℤ ×_F₁ Spec ℤ.
import JensenLadder.SelfProductRulings
-- T5 Gram backbone: primitive decomposition of any class by an ample ℓ (w=v−c•ℓ ⟂ ℓ, Qv=c²Qℓ+Qw)
-- — the split underneath hyperbolic_plane + hodge_index, turning signature into the Weil bound. RH-free.
import JensenLadder.PrimitiveDecomposition
-- T5 Weil-bound mechanism: Castelnuovo–Severi identity/inequality on the self-product (Γ=a•f₂+b•f₁+P ⟹
-- QΓ = ab·polar + QP, and ≤ ab·polar under QP≤0). The signature⟹eigenvalue-bound step (RH for curves).
import JensenLadder.CastelnuovoSeveri
-- Stage-0 two-piece reduction: faithful (integer boundary κ₋) vs measured (type-II density) positivity.
-- faithful⟹measured; measured⊉faithful (dust-blindness witness) ⟹ the package needs a faithful integer.
import JensenLadder.FaithfulMeasuredDichotomy
-- Capstone input (1) from Hermitian similarity: η=(S⁻¹)ᴴS⁻¹ positive quasi-Hermitizing metric when
-- M=SΛS⁻¹ with Λ Hermitian; restates the capstone as RH ⟸ (Mₙ similar-to-Hermitian ∀n) + convergence.
import JensenLadder.QuasiHermitianMetric
-- Cayley bridge: η-self-adjoint (ηM=Mᴴη) + M+i invertible ⟹ Cayley C=(M−i)(M+i)⁻¹ is η-unitary (CᴴηC=η).
-- Unifies the quasi-Hermitizing metric (input 1) with the Li/HP-Cayley unitary route; the BLIND (linear)
-- side — RH content is the quadratic/spectral-identification (input 2), not unitarity. RH-free.
import JensenLadder.CayleyUnitaryBridge
-- Cayley/Li atoms: FE acts as w↦1/w; on-line conjugate pairs give nonnegative Li contribution; real Cayley
-- transforms are automatically unitary. The off-line growth detector below uses these identities as its model.
import JensenLadder.LiCayleyAtom
-- Off-line Li detector core (quadratic off-line response): off-line FE-quartet |w|=r>1 ⟹ Li contribution
-- g(n)=4−2cos(nα)(rⁿ+r⁻ⁿ) is unbounded below for every phase; cos_half_recurrence discharges the
-- Diophantine near-return. The RH-content (quadratic) side; full-sum dominance remains open. RH-free.
import JensenLadder.OfflineLiDetector
-- Hermite–Hankel off-line-zero detector: the faithful companion C=diag(d)−u·1ᵀ has secular roots as
-- eigenvalues; κ₋=#non-real. Easy Hermite half (real roots ⟹ power-sum Hankel PSD; PT-unbroken ⟹
-- trace-Hankel PSD) ⟹ Hankel-PSD-FAILURE certifies failure of real diagonalizability; in the intended
-- simple-root companion setting this is the off-line-root certificate. A Schur-certifiable detector
-- (target = PSD of (tr Cⁱ⁺ᵏ)); third detector alongside OfflineLiDetector + complex_root_in_Phi_nonpos.
import JensenLadder.HermiteHankelDetector
-- Bezoutian = residue-Gram bivariate identity: A(s)B(t)−A(t)B(s) = (s−t)·∑ⱼ rⱼ Aⱼ(s)Aⱼ(t)
-- (gap-1 "analytic heart" of the faithful column; the matrix-coefficient form feeds
-- weighted_gram_posSemidef_iff ⟹ Bez⪰0 ⟺ residues≥0, the signed κ₋ read-off). RH-free, axiom-clean.
import JensenLadder.BezoutianResidue
